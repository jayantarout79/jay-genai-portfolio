# Gemini File Search — Streamlit Demo

1) `pip install -r requirements.txt`
2) `export GEMINI_API_KEY=your_key`  (or set in the UI sidebar)
3) `streamlit run app.py`
Upload a few PDFs or docs and ask questions. Answers will cite your sources when available.
