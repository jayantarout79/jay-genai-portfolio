# Pinecone Assistants Dashboard (Streamlit)

One-click dashboard to manage Pinecone Assistants: settings, files, and chat.

## Local
pip install -r requirements.txt
streamlit run app.py

## Deploy
Push to GitHub → Streamlit Cloud → set `PINECONE_API_KEY` in App secrets.